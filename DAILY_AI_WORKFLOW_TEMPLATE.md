# Daily AI 开发工作流模板（PayFiDemo）

> 目标：避免上下文漂移、范围失控、越做越乱。  
> 用法：每天直接复制模板给 AI，按阶段推进。

## 0) 每日固定输入（先填这个）

```text
项目：payfidemo
今日日期：YYYY-MM-DD
今日唯一目标（只能一个）：
- 例如：完成 release/submit 的错误路径测试

本轮允许修改：
- <file A>
- <file B>

本轮禁止修改：
- 合约 EIP-712 结构相关文件
- .env 与任何密钥文件
- docs（除非我明确要求）

完成定义（DoD）：
- [ ] 条件1（可验证）
- [ ] 条件2（可验证）
- [ ] 条件3（可验证）
```

---

## 1) 开工 Prompt（每天第一条）

```text
你是我的结对工程师。请严格执行以下流程，不要跳步：

【任务】
<粘贴今日唯一目标>

【硬约束】
1. 只允许改这些文件：<粘贴允许修改列表>
2. 明确禁止改：<粘贴禁止修改列表>
3. 不做重构，不新增依赖，不改命名风格
4. 不得输出“建议你去做”，你要直接执行

【执行步骤】
1) 先阅读相关代码并输出：
   - 当前实现现状（<=5条）
   - 风险点（<=3条）
   - 最小改动计划（<=7条）
2) 等我回复“继续”后再开始改代码
3) 改完后必须给出：
   - 修改文件清单
   - 每个文件改动目的
   - 剩余风险
4) 运行并汇报验证命令结果：
   - <测试命令1>
   - <测试命令2>

【完成定义】
<粘贴 DoD 清单>
```

---

## 2) 执行中 Prompt（每 30~60 分钟一次）

```text
请做一次中途校准，只输出以下四段：

1. 当前目标是否仍然单一明确（是/否 + 理由）
2. 现在的改动是否超出范围（是/否 + 列出超出项）
3. 若继续当前路线，最可能失败点是什么（最多3条）
4. 下一步最小动作（只允许一个动作）

如果发现范围漂移，请立即停止写代码，先给出“收敛方案”再继续。
```

---

## 3) 收工 Prompt（每天最后一条）

```text
请输出今日收工报告，严格使用这个格式：

【今日完成】
- ...

【未完成】
- ...

【代码证据】
- 文件：<path> -> 做了什么
- 文件：<path> -> 做了什么

【验证结果】
- 命令：<cmd> -> 结果：通过/失败（原因）

【明日第一任务建议（只能一个）】
- ...

【明日开工可直接使用的 Prompt】
<给出一段可复制的开工 prompt，包含允许/禁止修改范围>
```

---

## 4) 故障处理 Prompt（连续两次失败时）

```text
当前问题已连续失败两次。禁止继续盲修，请切换到“诊断模式”：

1. 先总结两次失败的根因假设（按概率排序）
2. 给出最小可验证实验（一次只做一个实验）
3. 每个实验包含：
   - 预期结果
   - 如果结果A，下一步是什么
   - 如果结果B，下一步是什么
4. 未完成实验前，不允许修改多个文件
```

---

## 5) PayFiDemo 专用“绝对不要动”清单（可直接贴给 AI）

```text
绝对不要动：
1. EIP-712 关键域和值：name=PayFiEscrowDemo, version=1, Release 字段顺序
2. 将 termsVersion 加入链上签名结构（禁止）
3. 混淆 intentId 与 escrowId 的语义
4. 提交或打印任何真实私钥/密钥
5. 无明确要求时扩展到多链、主网、真实 HSP 全联调
```

---

## 6) 每日节奏建议（轻量）

- 开工前 5 分钟：填「每日固定输入」
- 开发 45 分钟：执行
- 校准 5 分钟：跑一次「执行中 Prompt」
- 循环 2~4 轮
- 收工 10 分钟：跑「收工 Prompt」，把结果同步到 `WORKLOG.md`

---

## 7) 开发协作提示词模板库（可复制）

> 建议：一个项目保留一个“主聊天”，单次复杂任务可开“专题聊天”；结束后把结论贴回主聊天，避免上下文漂移。

### A. 日常开发模板（小步快跑）

```text
你是我的结对工程师。请按“先对齐、再改动、再验证”的顺序执行。

【目标】
- 本轮唯一目标：<一句话>

【范围】
- 允许修改：<files>
- 禁止修改：<files>
- 不允许新增依赖/不允许大规模重构（除非我明确批准）

【先对齐（先不要改代码）】
请先输出：
1) 你对目标的理解（<=3条）
2) 你将修改的文件与理由
3) 最小可行步骤（<=5步）
4) 验收方式（命令 + 预期结果）

等我回复“继续”后再改代码。

【实施要求】
- 每次只做一个小步骤，步骤完成后报告变更
- 若发现需求冲突或信息不足，立即停止并提问
- 不要输出空泛建议，直接执行可执行动作

【交付格式】
1) 改动文件清单
2) 每个文件改了什么/为什么
3) 验证命令和结果
4) 风险与下一步（最多3条）
```

### B. Bug 排查模板（先定位再修复）

```text
进入“诊断优先”模式。当前问题如下：
<粘贴报错/现象/日志>

【目标】
- 找到可复现根因并给出最小修复

【规则】
1) 先复现，后修复；禁止盲改
2) 一次只验证一个假设
3) 没有证据不要下结论

【请先输出】
1) 问题复述（你理解到的现象与影响）
2) 根因假设列表（按概率排序，最多5条）
3) 最小实验计划（每次一个实验，含命令与预期）

我回复“继续”后你再执行实验与修复。

【修复后必须输出】
- 根因证据（哪个实验确认了什么）
- 修复点（文件 + 改动目的）
- 回归验证（命令 + 结果）
- 防复发建议（测试或守护规则，最多3条）
```

### C. 重构模板（受控重构）

```text
我们要做“受控重构”，目标如下：
<目标，例如：降低模块耦合/拆分过长函数>

【硬约束】
- 不改变对外行为（API/事件/存储结构不变）
- 不新增业务需求
- 变更可回滚、可验证

【先输出重构设计（先不要改代码）】
1) 当前痛点（证据化，最多5条）
2) 重构边界（会改/不会改）
3) 分阶段方案（Phase 1/2/3，每阶段可独立提交）
4) 每阶段验收标准（测试/编译/静态检查）

我回复“继续”后再执行 Phase 1。

【执行要求】
- 每阶段结束都输出：改动摘要 + 验证结果 + 风险
- 若任一阶段验证失败，先回退到可运行状态再继续

【最终交付】
- 重构前后结构对比（简述）
- 行为等价证明（测试/关键路径验证）
- 仍未解决的技术债（最多3条）
```

### D. 提交模板（Commit / PR 一体）

```text
现在进入“提交准备”阶段。请基于当前工作区改动，生成可直接使用的提交材料。

【目标】
- 产出：1) commit message 2) PR 描述 3) 测试清单 4) 回滚方案

【约束】
1) 只描述真实发生的改动，不要虚构
2) 先说明“为什么改”，再说明“改了什么”
3) 若有风险，必须明确写在 PR 描述里
4) 若测试未全过，必须显式标注

【请先输出】
1) 变更摘要（<=5条）
2) 影响范围（功能、接口、数据、部署）
3) 风险清单（最多3条）
4) 建议的 commit message（Conventional Commits 风格）

【然后输出可复制内容】

---COMMIT MESSAGE---
<type>(<scope>): <subject>

<body: 解释改动动机、关键实现、兼容性影响>
---END---

---PR TITLE---
<一句话标题>
---END---

---PR BODY---
## Why
- ...

## What Changed
- ...

## Impact
- 功能影响：...
- 接口影响：...
- 数据/存储影响：...

## Test Plan
- [ ] <命令1>（结果：通过/失败）
- [ ] <命令2>（结果：通过/失败）

## Risks
- ...

## Rollback
- 回滚步骤1：...
- 回滚步骤2：...
---END---

【最后再输出】
- 是否可以提交（是/否）
- 若“否”，给出最小补救动作（只给一个）
```

### E. 热修复提交模板（Hotfix）

```text
现在进入“热修复提交”模式。请按生产事故标准输出，不要省略风险与回滚信息。

【事故背景】
- 事故编号（可选）：<INC-xxx>
- 现象：<一句话>
- 首次发现时间：<YYYY-MM-DD HH:mm>
- 影响范围：<用户/接口/链路>
- 当前状态：<持续中/已缓解/待验证>

【目标】
- 在最短时间内恢复可用性，同时控制二次故障风险

【硬约束】
1) 仅允许最小必要改动，禁止顺手重构
2) 必须提供可执行回滚步骤
3) 必须标注“未覆盖测试”与残余风险
4) 所有结论都要有证据（日志/监控/复现）

【请先输出】
1) 根因结论（若未完全确认，给“当前最可信假设”）
2) 修复策略（为何选这个最小方案）
3) 风险评估（功能风险/性能风险/数据风险）
4) 发布策略（全量 or 灰度，灰度阈值与观察指标）

【然后输出可复制内容】

---COMMIT MESSAGE---
fix(hotfix): <一句话修复点>

<为什么这是最小必要修复>
<影响范围与兼容性说明>
<若存在临时性方案，写明后续清理计划>
---END---

---PR TITLE---
[HOTFIX] <故障现象短标题>
---END---

---PR BODY---
## Incident
- 编号：<INC-xxx>
- 现象：...
- 影响范围：...
- 发现时间：...

## Root Cause
- ...
- 证据：...

## Fix
- ...
- 为什么是最小改动：...

## Risk Assessment
- 功能风险：...
- 性能风险：...
- 数据一致性风险：...

## Rollout Plan
- 发布方式：全量/灰度
- 灰度步骤：
  1. ...
  2. ...
- 观察指标与阈值：
  - 错误率：...
  - 延迟：...
  - 关键业务成功率：...

## Rollback Plan
- 触发条件：...
- 回滚步骤：
  1. ...
  2. ...
- 回滚后验证：
  - <命令/监控项1>
  - <命令/监控项2>

## Test Evidence
- [ ] <最小回归测试1>（通过/失败）
- [ ] <最小回归测试2>（通过/失败）
- 未覆盖项：...

## Follow-ups (Non-Hotfix)
- [ ] 后续治理任务1
- [ ] 后续治理任务2
---END---

【最后输出】
- 是否满足热修复上线条件（是/否）
- 若“否”，当前唯一阻塞项是什么
```

